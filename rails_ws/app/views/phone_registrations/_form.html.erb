<%= form_for(@phone_registration) do |f| %>
  <% if @phone_registration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phone_registration.errors.count, "error") %> prohibited this phone_registration from being saved:</h2>

      <ul>
      <% @phone_registration.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :device_ident %><br />
    <%= f.text_field :device_ident %>
  </div>
  <div class="field">
    <%= f.label :registration_ident %><br />
    <%= f.text_field :registration_ident %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
